import API_ENDPOINT from './index';

const REGISTER_API_ENDPOINT = `${API_ENDPOINT}/auth/register`;
const LOGIN_API_ENDPOINT = `${API_ENDPOINT}/auth/login`;

export default {
    REGISTER_API_ENDPOINT,
    LOGIN_API_ENDPOINT
}